server:
  port: 9090

spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/Java12CacheDemo
    username: postgres
    password: 1234

  jpa:
    hibernate:
      ddl-auto: update

  cache:
    type: redis
  data:
    redis:
      host: localhost
      port: 6379
#    redis:
#      time-to-live: 60000
